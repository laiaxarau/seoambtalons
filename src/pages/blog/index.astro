---
import BaseLayout from "../../layouts/BaseLayout.astro";
import { Image } from "astro:assets";
import FormattedDate from "../../components/common/FormattedDate.astro";
import { sanity } from "../../lib/sanity";
import blogPlaceholder from "../../assets/blog-placeholder-about.jpg";

type Post = {
  title: string;
  slug: {
    current: string;
  };
  publishedAt: string;
  mainImage?: {
    asset: {
      url: string;
    };
  };
  body: { children: { text: string }[] }[];
};

const posts: Post[] = await sanity.fetch(`
  *[_type == "post"] | order(publishedAt desc){
    title,
    slug,
    publishedAt,
    mainImage,
    body
  }
`);

// const posts = sortPostsByDate(await getCollection("blog"));
---

<BaseLayout
  title="Blog - SEO amb Talons"
  description="Articles i tutorials sobre SEO, posicionament web i estratègies digitals per a dones emprenedores."
>
  <section class="px-8 lg:px-16 py-24 lg:py-32">
    <div class="max-w-7xl mx-auto">
      <!-- Capçalera -->
      <div class="text-center mb-16">
        <h1
          class="text-4xl lg:text-5xl font-display font-bold text-offwhite mb-4"
        >
          Blog SEO amb Talons
        </h1>
        <p class="text-sand text-lg max-w-2xl mx-auto">
          Articles, tutorials i estratègies per posicionar el teu negoci a
          Google sense perdre el teu estil.
        </p>
      </div>

      <!-- Grid de posts -->
      <ul class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
        {
          posts.map((post) => (
            <li class="group">
              <a
                href={`/blog/${post.slug.current}/`}
                class="block bg-white/5 border border-white/10 rounded-lg p-6
                       hover:bg-white/10 hover:border-coral/50 transition-all duration-300"
              >
                <div class="mb-4 overflow-hidden rounded-lg">
                  {post.mainImage ? (
                    <Image
                      width={400}
                      height={200}
                      src={post.mainImage.asset.url}
                      alt={post.title}
                      class="w-full h-48 object-cover group-hover:scale-105 transition-transform duration-300"
                    />
                  ) : (
                    <Image
                      width={400}
                      height={200}
                      src={blogPlaceholder}
                      alt={post.title}
                      class="w-full h-48 object-cover group-hover:scale-105 transition-transform duration-300"
                    />
                  )}
                </div>
                <h3
                  class="font-display text-xl font-bold text-offwhite mb-2
                           group-hover:text-coral transition-colors duration-300"
                >
                  {post.title}
                </h3>
                <p class="text-sand text-sm">
                  {post.publishedAt && (
                    <FormattedDate date={new Date(post.publishedAt)} />
                  )}
                </p>
                {post.body.length > 0 && post.body[0]?.children?.length > 0 && (
                  <p class="text-sand/80 text-sm mt-2 line-clamp-2">
                    {post.body[0].children
                      .map((child) => child.text)
                      .join(" ")
                      .slice(0, 150)}
                    ...
                  </p>
                )}
              </a>
            </li>
          ))
        }
      </ul>
    </div>
  </section>
</BaseLayout>
