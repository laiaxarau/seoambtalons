<section class="px-8 lg:px-16 py-24 text-center relative overflow-hidden">

  <!-- Gradient radial -->
  <div class="absolute inset-0 pointer-events-none"
    style="background: radial-gradient(ellipse 60% 60% at 50% 50%, rgba(232,71,42,0.12) 0%, transparent 70%)">
  </div>

  <div class="relative z-10 max-w-2xl mx-auto">
    <p class="eyebrow justify-center mb-3">Ara és el moment</p>
    <h2 class="font-display text-4xl lg:text-5xl font-black text-offwhite leading-tight mb-6">
      El teu negoci mereix<br>ser trobat per qui<br>
      <em class="italic text-coral">realment l'estima</em>
    </h2>
    <p class="text-sand font-light mb-10 max-w-md mx-auto">
      Deixa de dependre de les xarxes socials. Construeix una presència
      orgànica que treballi per tu, cada dia.
    </p>
    <a href="#reserva" class="btn-primary">
      Reserva la sessió gratuïta →
    </a>
  </div>
</section>

<!-- <style>
  .eyebrow {
    @apply inline-flex items-center gap-2 text-coral text-xs uppercase tracking-[0.14em] font-medium;
  }
  .eyebrow::before { content: ''; @apply block w-5 h-px bg-coral; }
</style> -->